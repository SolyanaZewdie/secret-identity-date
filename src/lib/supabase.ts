/**
 * LELA uses the generated Lovable Cloud client everywhere.
 * This module stays as a thin re-export so existing imports keep working.
 */
export { supabase } from "@/integrations/supabase/client";
